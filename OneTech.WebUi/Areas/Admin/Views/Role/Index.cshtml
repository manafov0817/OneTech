@model IEnumerable<IdentityRole>
<div class="container">
    <div class="page-content">
        <div class="page-title">
            <div class="row">
                <div class="col-md-6">
                    <a href="#">E-Commerce</a>
                    <span>Options</span>
                </div>
                <div class="col-md-6 d-md-flex d-none align-items-center justify-content-end">
                    <a class="btn btn-sm btn-primary-soft br-3 p-x-15 mr-2" href="@Url.Action("Create","Role")">Add</a>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <div class="card company">
                    <div class="card-body table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th scope="col">Id</th>
                                    <th scope="col">Role Name</th>
                                    <th scope="col">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Count( ) > 0)
                                {
                                    @foreach (var role in Model)
                                    {
                                        <tr>
                                            <td>
                                                @role.Id
                                            </td>
                                            <td>
                                                @role.Name
                                            </td>                                            
                                            <td>
                                                <div class="btn-group dropleft">
                                                    <i class="icon-options-vertical btn" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                                        <span class="sr-only">Toggle Dropdown</span>
                                                    </i>
                                                    <div class="dropdown-menu">
                                                        <a class="dropdown-item" href="@Url.Action("Edit","Role",new { RoleId = role.Id})">Edit</a>
                                                        <a onclick="return confirm('Are you sure to delete?')" class="dropdown-item" href="@Url.Action("Delete","Option", new { RoleId = role.Id})">Delete</a>
                                                    </div>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
